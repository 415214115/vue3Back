import routerList from '../../navSlider'

const nav: object  = {
	state: {
        routers: routerList
	},
	mutations: {
		// setToken: (state: any, self: any) => {
		// 	state.token = self
		// }
	},
	actions: {
		
	}
}
export default nav
